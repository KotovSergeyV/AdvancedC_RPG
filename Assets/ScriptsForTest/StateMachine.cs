using System;
using System.Collections.Generic;
using UnityEngine;

public class StateMachine
{
    IState currentState;
    Player player;

    Dictionary<TypeOf(IState), List<(bool Func<IState>, IState Funk<IState>)> > stateTransitionTable;


    void tryChangeState() { }
}
